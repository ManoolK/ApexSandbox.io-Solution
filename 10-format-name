public String formatName(String firstName, String lastName) {
    String result = String.isBlank(lastName) ? '' : lastName;
    if (String.isBlank(result)) {
    	return result += String.isEmpty(firstName) ? '' : firstName;
    } else {
        return result += String.isEmpty(firstName) ? '' : ', ' + firstName;
    }
}
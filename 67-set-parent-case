public void linkParent(Case c1, Case c2) {
    if (c1?.Contact == c2?.Contact) {
        if (c1.CreatedDate <= c2.CreatedDate) {
            c2.ParentId = c1.Id;
            update c2;
        } else {
            c1.ParentId = c2.Id;
            update c1;
        }
    }
}